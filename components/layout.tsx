import React, {FunctionComponent} from "react";

interface IProps {
    title: string
    children: React.ReactNode
}
// const Layout: FunctionComponent<IProps> = ({ children, title }) => {
//     return (
//         // <Menu />
//     {children}
//         )
// }

export default Layout
